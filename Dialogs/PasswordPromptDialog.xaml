<Window x:Class="WpfApp.PasswordPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Certificate Password" Height="180" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1E1E1E" Foreground="#E0E0E0"
        FontFamily="Consolas">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Enter the certificate password:" Margin="0,0,0,10"/>
        <PasswordBox Grid.Row="1" x:Name="txtPassword"
                     Background="#2D2D30" Foreground="#E0E0E0"
                     BorderThickness="1" BorderBrush="#3C3C3C"
                     FontFamily="Consolas" Height="28"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" Click="BtnOk_Click" Width="80" Margin="5"
                    Background="#0E639C" Foreground="#E0E0E0" BorderThickness="0"/>
            <Button Content="Cancel" Click="BtnCancel_Click" Width="80" Margin="5"
                    Background="#3C3C3C" Foreground="#E0E0E0" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
